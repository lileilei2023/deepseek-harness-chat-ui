# 打磨欠账

按「用起来有多难受」排序，每条写清**现状是什么、为什么不好用、做到什么算完**。
所有论断都是从代码或实测里得来的，标了文件和行为，接手的人不需要先复现一遍。

约定：`SB.tsx` = `source/client-ui-skill-chat/src/client/SkillContactsBrowser.tsx`，
`catalog.ts` = `source/workbuddy-skill-catalog/src/index.ts`。

> 2026-09-06：按 `docs/项目改进方案-2026-09.md` 做了一轮。已完成的条目移到文末
> 「已还的账」，只留下判断依据与遗留的部分；下面是剩下的。

---

## P0 · 产物这条链还差一段

### 1. 产物没有版本，看不出「变了什么」

现在能看到是谁、第几步产生的，也能下载和在新标签打开，但一份报告第二次生成后
仍然只看得到最终内容。`roomArtifacts` 已经回传 `revisions`（同一路径被写过几次），
界面也显示了次数，但没有任何一版的历史内容。

**难点**：要 diff 就得有旧内容。三条路：

- **走 git**：工作区通常是仓库，`git log --follow -- <path>` + `git show` 就是
  真历史，不用自己存。代价是非仓库工作区没有这个能力。
- **存快照**：状态文档已经 337 KB 且整份读写，再塞进内容会更糟。
- **存 diff**：产生时算一次 diff 存下来，比存全文小得多，但仍在同一份文档里。

**做到什么算完**：同一路径的产物有版本列表，相邻两版可 diff（`DiffView` 已可
复用），「恢复到此版本」写回文件并二次确认。

---

## P1 · 群聊还差一件事

### 2. 对比模式只做到了「让他们各答一次」

`@所有人` 会写入一段广播指令，协调者会让每位成员各答一次。但结果仍是顺序转述的
一段文字，不是并排的卡片，也没有「采纳某一条作为后续上下文」。

**难点**：并排渲染需要接管宿主的流节点渲染（`conversation.chat.node` 是按
entryKey 路由的可替换插槽），代价是要重新实现思考与工具调用的富渲染——第四轮
评审已经因为同样的原因放弃过一次折叠。

**做到什么算完**：一次提问 N 个成员各答一次，结果并排，可点「采纳」。

---

## P2 · 工作台

### 3. 文件树没有「新建 / 重命名 / 删除」

只读。产物做出来之后想整理一下都做不到。右键菜单已经有「加入对话 / 复制路径 /
在文件夹中显示 / 在这里打开终端」，缺的是写操作。

**注意**：写操作要走宿主的工作区边界检查（`browseProject`、`readProjectFile`、
`revealProjectPath` 都已有 `isWithin(root, target)` 的先例），删除要二次确认。

### 4. 终端没有 PTY 尺寸

从没调用过 resize，任何按宽度绘制的东西（`top`、进度条、`tmux`、表格）都会错行。

**这个我们做不了**：宿主的 `TerminalSpawnRequest` 只有 `type / name / cwd`，
契约里根本没有 resize。要么向 Harness 提 issue，要么等它开放。

### 5. 终端不能交互

按键不直通，所以任何交互式提示（`y/n`、密码、`git rebase -i`）仍然卡死。
Ctrl-C 已经可以（走 `signalSkillChatTerminal`），上下方向键的历史也有了，
但 Tab 补全和「把按键原样送进去」还没有。

**做到什么算完**：至少 Ctrl-D 与方向键直通，交互式提示能回答。

---

## P3 · 对话与房间

### 6. `@` 提及的 UI 路径没有被自动化验证过

无头浏览器驱动不了 Lexical 编辑器，所以候选列表、插入、序列化这条链只在数据层
验证过（`memberSkillName`、`memberForSubagent` 有单测）。**这条路径目前没有
端到端测试**。

### 7. 房间状态是整份文档读写

`putSkillChatState` 每次写整个文档（上限 2 MiB）。房间、persona 和现在新增的
运行记录多了以后，每一次改动都要序列化全部内容。实测 337 KB 时还撑得住，但这是
线性劣化。

**做到什么算完**：按集合分文件（rooms / roomSessions / personas / automations /
automationRuns），或至少 debounce + 差量。

### 8. 自动化的周期规则只有 `every:N(m|h|d)`

「每天早上九点」表达不了——只能说「每 24 小时」，于是首次运行时间一旦漂移，
之后每次都跟着漂。运行记录、失败重试、桌面通知都已经有了，缺的是这一条。

**做到什么算完**：支持 `daily@09:00`、`weekdays@09:00`，并按本地时区计算。

---

## P4 · 工程

### 9. `SB.tsx` 已经 3000 行

侧栏、房间、工作台、终端、浏览器、成员面板、菜单、弹窗全在一个文件里。这一轮
又往里加了成员活动、产物溯源、运行记录，比之前更长了。该拆了——按面板拆最自然
（`workbench/`、`roster/`、`room/`、`automation/`）。

### 10. 客户端仍然没有组件渲染测试

52 项测试里，客户端部分是纯函数：`parseDiff`、`preferLocalState`、
`bindLegacyGroups`、`parseAnsiLines`、`migrateMemberKeys`、`memberForSubagent`、
`oneLineBio`。**没有任何组件渲染测试**。

### 11. 三张截图还欠一次带模型的重拍

`scripts/shoot.mjs` 已经进仓库（用法见 CLAUDE.md）。这一轮用它重拍了 `themes`
（旧图还画着第七轮已撤回的「夜航」皮肤，是错的）、`groups`（能看到一句话简介的
改进），并新增了 `members`。

`main`、`dark`、`automations` **没有换**：演示实例没有 API Key，会话区里是一条
「本轮运行失败 · no API key」的红色报错，放进 README 会让人以为产品是坏的。这三张
要等一次配了模型的演示实例再拍。

`record.mjs`（README 的 GIF）仍不在仓库里。

---

## 排查时的经验

- **务必 `dsh web --no-open`**，否则用户的真实浏览器标签页会用陈旧的
  localStorage 覆盖服务端状态。这个坑让我误判过一次「数据丢失」。
- 无头浏览器探针每次跑完要杀干净（`ps -x | grep headless=new`），残留的实例会
  继续写状态。
- 改了 `package.json` 依赖，顺序是：先跑一次 `npm run rebuild`（它会把包物化进
  Harness 检出，这一步会因 lockfile 过期而失败），再在 Harness 检出里
  `CI= pnpm install --no-frozen-lockfile`，然后重跑 rebuild。反过来无效。
- 想确认某个宿主能力到底存不存在，**实测比读文档快**：直接 curl 或起一个探针，
  404 和 400 的区别往往就是答案。
- **`content-visibility` 会让 `getComputedStyle` 失真。** 被跳过的子树里读到的
  颜色可能是另一套主题的值。这一轮的对比度审计因此误报过一批「暗色下白底白字」，
  截图才是准的；要用计算样式测量就先临时 `*{content-visibility:visible!important}`。

---

## 已还的账（2026-09-06）

| 原条目 | 结果 |
| --- | --- |
| 终端「一条命令一次快照」 | 改为轮询的流；ANSI 自带解析器；Ctrl-C／停止；命令历史；截断提示与加载更早；无 PTY 的会话也改成了流式 |
| 产物判定是启发式的 | 改为读会话日志里的 write/edit 调用，带 seq、成员与生成次数；读不到日志才退回 mtime 并在界面说明 |
| 产物不能下载／新标签打开／分享 | Blob 下载、新标签打开、在文件夹中显示；截断与二进制不提供下载 |
| 浏览器面板地址硬编码 | 默认为空并给提示，按工作区记住上次地址 |
| 成员 id 不稳定 | 状态升到 v3，成员与 persona 改用 Skill 名作键，三处按名回退删除 |
| 内容搜索全量读文件 | 走 PATH 上的 ripgrep（`--json`），取不到时回退；实测 37ms |
| 目录树没有虚拟化 | `content-visibility` + 实测行高，与另两个列表一致 |
| 工作台状态不持久 | 按房间记住面板、展开目录与文件标签 |
| 自动化只有「立即运行」和「暂停」 | 运行记录、未读数、失败可见与重试、桌面通知 |
| 群里看不到谁在干活（新发现） | 成员面板、正在工作条、点名 @、群公告、入群提示 |
