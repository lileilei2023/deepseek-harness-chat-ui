# 打磨欠账

表面功能已经铺开，细节欠得很多。下面按「用起来有多难受」排序，每条写清**现状
是什么、为什么不好用、做到什么算完**。所有论断都是从代码或实测里得来的，标了
文件和行为，接手的人不需要先复现一遍。

约定：`SB.tsx` = `source/client-ui-skill-chat/src/client/SkillContactsBrowser.tsx`，
`catalog.ts` = `source/workbuddy-skill-catalog/src/index.ts`。

---

## P0 · 终端根本不能用

这是目前最差的一块。不是缺功能，是**交互模型错了**：它被做成了「发一条命令，
拿一次快照」，而终端是一个持续的流。

### 1. 命令跑完之前看不到任何输出

`catalog.ts` 的 `sendSkillChatTerminal`：

```ts
const operation = terminals.startSend(agent, terminalId, { text, submit: true })
await operation.done          // 这是「按键送达」，不是「命令结束」
const output = terminals.read(agent, terminalId, { count: 2_000 })
```

`operation.done` 表示输入送达，然后**读一次**缓冲就返回。此后除非你再发一条
命令，界面不会再更新。所以 `npm install`、`pytest`、任何跑几秒以上的东西，你
看到的就是一个提示符，然后一片死寂。

**做到什么算完**：输出是推送或轮询上来的，命令运行期间界面持续增长；能看出
「还在跑」和「跑完了」。宿主的 `terminals.read(agent, id, { count })` 可以反复
调用，最低成本是客户端起一个轮询（运行中 ~300ms，空闲退避），更好的做法是查
`terminals` 服务有没有订阅接口。

### 2. ANSI 转义原样打印

`SB.tsx` 里 ANSI 相关代码是 **0 行**，输出直接塞进 `<pre>`。任何带颜色的工具
（git、npm、pytest、ls --color）都会喷出 `[0m[32m` 这类字符。

**做到什么算完**：颜色、粗体、光标行内擦除（`\r`）正确渲染。别自己写解析器，
`ui-primitives` 或 Harness 的终端相关包里可能已经有；先找。

### 3. 只能发完整命令行，不能交互

输入是一个单行 `<input>`，提交才发送。于是：

- 任何交互式提示（`y/n`、密码、`git rebase -i`）都卡死，没法回答；
- 没有 Ctrl-C，跑飞了的命令停不下来；
- 没有上下方向键的历史，没有 Tab 补全。

**做到什么算完**：按键直通（至少 Ctrl-C / Ctrl-D / 回车 / 方向键），本地命令
历史，中断可用。

### 4. 没有 PTY 尺寸

从没调用过 resize。任何按宽度绘制的东西（`top`、进度条、`tmux`、表格）都会错行。

**做到什么算完**：面板尺寸变化时把行列数同步给后端。

### 5. 滚动缓冲固定 2000 行且不可配

`count: 2_000` 硬编码在两处。长构建日志会被无声截断，`truncated` 标志虽然回传
了但界面没有任何提示。

**做到什么算完**：至少在界面上显示「已截断」，理想是可以往上加载更多。

---

## P1 · 产物这条链还很粗糙

### 6. 「产物」的判定是启发式的，会误伤也会漏

`catalog.ts` 的 `recentProjectFiles` 按 mtime 过滤，客户端用扩展名白名单
（`isDeliverable`）区分文档与数据。两处都不精确：

- 房间运行期间**任何**改动都算，包括你自己在别的窗口编辑的文件、其它工具写的
  东西；
- 白名单之外的产物（`.svg`、`.ipynb`、`.parquet`、无扩展名）被降级进「其它文件」；
- 同一份报告重新生成会出现两条，没有去重或版本概念。

**做到什么算完**：产物能追溯到**是哪条消息、哪个成员产生的**。可行路径是在
`tool/code-dispatch` 事件里认出写文件的调用（`write`、`edit`、以及 `bash` 里
的重定向），把路径记进 `RoomSession`。那才是真的因果，mtime 只是相关。

### 7. 产物不能下载、不能在新标签打开、不能分享

只能在面板里看。一份 47 KB 的自包含 HTML 报告，用户想发给别人时无路可走。

**做到什么算完**：下载、在新标签打开。注意工作区文件没有 HTTP 地址，
`file://` 在 http 源下会被拦——要么宿主加一条受限的静态路由，要么用 Blob URL。

### 8. 产物没有「变了什么」

只能看到最终内容。一份报告第二次生成后，看不出改了哪里。

**做到什么算完**：产物有版本，能对前一版做 diff。工作台里已经有 `DiffView`
可复用。

---

## P2 · 工作台的其它粗糙处

### 9. 浏览器面板的默认地址是硬编码的

`SB.tsx:1080` 起：`useState('http://127.0.0.1:56517/')`。这个端口没有任何来由，
用户打开面板看到的是一个必然打不开的地址。

**做到什么算完**：默认为空并给出提示，或者去问宿主有没有正在跑的开发服务器。

### 10. 目录树没有虚拟化

`renderEntries` 一次性渲染全部展开项，实测展开一个目录就到了 262 行。大仓库
展开几层会卡。`.list` 上用了 `content-visibility: auto`，树没有。

**做到什么算完**：树在数千节点下滚动流畅。

### 11. 内容搜索是全量读文件

`searchProjectFiles` 在 `contents` 模式下逐个 `readFile`，上界是 4000 个文件
× 512 KB。第一次搜可能要好几秒，而且没有进度提示，界面只显示「搜索中…」。

**做到什么算完**：有增量结果或进度；理想是走 ripgrep 一类的外部工具（宿主有
subprocess 服务）。

### 12. 文件树没有「新建 / 重命名 / 删除」

只读。产物做出来之后想整理一下都做不到。

### 13. 工作台状态不持久

关掉面板再打开，展开的目录、打开的文件标签、终端标签全没了。房间状态是持久
化的，工作台状态不是。

---

## P3 · 对话与房间

### 14. 成员 id 不稳定，只有回退没有根治

见 `CLAUDE.md`「会咬人的几件事」。三处按名回退是创可贴。**正确做法是房间存
Skill 名而不是带 root 前缀的 id**，需要一次状态迁移。

### 15. `@` 提及的 UI 路径没有被自动化验证过

无头浏览器驱动不了 Lexical 编辑器，所以候选列表、插入、序列化这条链只在数据层
验证过（`memberSkillName` 有单测）。**这条路径目前没有端到端测试**。

### 16. 房间状态是整份文档读写

`putSkillChatState` 每次写整个文档（上限 2 MiB）。房间和 persona 多了以后，每
一次改动都要序列化全部 358 个 persona。目前 337 KB，还能撑，但这是线性劣化。

### 17. 自动化只有「立即运行」和「暂停」

没有运行历史、没有失败重试、没有结果通知。定时到点跑完了，用户不会知道。

---

## P4 · 工程

### 18. `SB.tsx` 已经 2400 行

侧栏、房间、工作台、终端、浏览器、菜单、弹窗全在一个文件里。该拆了——按面板
拆最自然（`workbench/`、`roster/`、`room/`）。

### 19. 客户端几乎没有单元测试

32 项测试里绝大多数是宿主端目录扫描与状态持久化。客户端只有 `parseDiff`、
`preferLocalState`、`bindLegacyGroups` 等纯函数，**没有任何组件渲染测试**。

### 20. 演示媒体是手工流程

`scratchpad` 里的 `shoot.mjs` / `record.mjs` 不在仓库里，每次改 UI 都要手工重录
README 的截图和 GIF。应该收进 `scripts/` 并写进文档。

---

## 排查时的经验

- **务必 `dsh web --no-open`**，否则用户的真实浏览器标签页会用陈旧的
  localStorage 覆盖服务端状态。这个坑让我误判过一次「数据丢失」。
- 无头浏览器探针每次跑完要杀干净（`ps -x | grep headless=new`），残留的实例会
  继续写状态。
- 改了 `package.json` 依赖，先在 Harness 检出里 `pnpm install --no-frozen-lockfile`。
- 想确认某个宿主能力到底存不存在，**实测比读文档快**：直接 curl 或起一个探针，
  404 和 400 的区别往往就是答案。
